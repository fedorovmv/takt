---
provider: coding-agent
model: implementation
---

Generate or modify the requested Remotion composition in the current repository.

Inspect the existing Remotion project, assets, conventions, dimensions, frame rate, and rendering setup. Plan scenes and timing, implement reusable typed components, respect asset licenses, and keep animation deterministic. Run type checks, tests, and a representative render or still-frame validation. Record commands and output paths in `$ARTIFACTS_DIR/remotion.md`.

User request:

$ARGUMENTS
