---
assistant: opencode
model: review
---

Attempt to reproduce the reported problem using the current checkout and the safest relevant tools.

Use the investigation, run focused commands, capture stdout/stderr, versions, logs, and screenshots when applicable, and avoid destructive production actions. Save evidence under `$ARTIFACTS_DIR/reproduction/` and a summary to `$ARTIFACTS_DIR/reproduction.md`. Return only the JSON requested by the workflow, with `reproduced`, `summary`, and `evidence_path`.

User request:

$USER_MESSAGE
