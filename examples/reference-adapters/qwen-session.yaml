apiVersion: takt/v1alpha1
kind: Workflow
metadata:
  name: qwen-reference-session

defaults:
  assistant: qwen-reference
  model: coding
  session: fresh

nodes:
  - id: inspect
    prompt: |
      Inspect the current repository and return a concise factual summary.
