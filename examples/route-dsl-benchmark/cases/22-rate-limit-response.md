Вызвать внешний HTTP API. Для ответа 429 использовать Retry-After, если такой механизм поддерживается DSL, и ограничить число повторов. Если Retry-After нельзя выразить штатными средствами, применить ближайшую поддерживаемую политику и явно не придумывать новый параметр.
