apiVersion: takt/v1alpha1
kind: Config

models:
  default:
    provider: openai
    id: gpt-5.6
    params:
      reasoning_effort: high

assistants:
  pi:
    type: pi
    binary: pi
    project_trust: deny
    max_output_bytes: 2097152
