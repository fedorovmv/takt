apiVersion: takt/v1alpha1
kind: Workflow
metadata:
  name: write-item
nodes:
  - id: append
    bash: |
      printf '%s\n' '${inputs.name}' >> order.txt
  - id: result
    depends_on: [append]
    bash: printf '%s' '${inputs.name}'
